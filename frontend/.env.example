# Google Maps JavaScript API key (browser-exposed).
# Required for store address maps + the dynamic 送り・足代 zone map.
# Must be restricted to your domain via HTTP Referrer in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=
